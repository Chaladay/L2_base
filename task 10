function bingo(ticket, win){
    let wins = 0;
    for (let i = 0; i < ticket.length; i++) {
        let [str, num] = ticket[i];
        for (let char of str) {
            if (char.charCodeAt(0) === num) {
                wins++;
                break; 
            }
        }
    } 
    if (wins >= win) {
        return "Winner!";
    } else {
        return "Loser!";
    }
}
